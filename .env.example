# Required: API key for authenticating requests to the gateway
# Generate with: openssl rand -hex 32
CLAUDE_CODE_GATEWAY_API_KEY=your-secret-key-here

# Optional: Server port (default: 3100)
PORT=3100

# Authentication (one of these is required for Claude CLI)
# Option 1: OAuth token for Claude Max subscribers
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token

# Option 2: Anthropic API key
# ANTHROPIC_API_KEY=your-anthropic-key

# Custom environment variables for Claude skills
# Any variable prefixed with CLAUDE_CODE_GATEWAY_ passes through to Claude CLI
# CLAUDE_CODE_GATEWAY_MY_API_URL=https://api.example.com
# CLAUDE_CODE_GATEWAY_MY_API_KEY=secret
